<%= form_with(model: public_servant, local: true) do |form| %>
  <% if public_servant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(public_servant.errors.count, "error") %> prohibited this public_servant from being saved:</h2>

      <ul>
      <% public_servant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name %>
  </div>

  <div class="field">
    <%= form.label :document %>
    <%= form.text_area :document %>
  </div>

  <div class="field">
    <%= form.label :position %>
    <%= form.text_area :position %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age %>
  </div>

  <div class="field">
    <%= form.label :period %>
    <%= form.text_area :period %>
  </div>

  <div class="field">
    <%= form.label :salary %>
    <%= form.number_field :salary %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
